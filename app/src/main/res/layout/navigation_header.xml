<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="182dp"
              android:background="?attr/colorPrimaryDark"
              android:padding="16dp"
              android:theme="@style/ThemeOverlay.AppCompat.Dark"
              android:orientation="vertical"
              android:gravity="bottom">
  <ImageView
      android:id="@+id/nav_header_avatar"
      android:layout_width="100dp"
      android:layout_height="80dp"
      android:layout_marginLeft="-10dp"
      android:src="@mipmap/ic_launcher_round"
      android:layout_above="@+id/nav_header_text"
      android:layout_alignParentLeft="true"/>

  <TextView
      android:id="@+id/nav_header_text"
      android:layout_marginTop="20dp"
      android:textSize="18sp"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_above="@+id/nav_header_subtext"
      android:text="@string/navigation_header_title"/>
  <TextView
      android:id="@+id/nav_header_subtext"
      android:textSize="16sp"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="@string/navigation_header_subtitle"/>
</LinearLayout>